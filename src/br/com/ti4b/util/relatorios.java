package br.com.ti4b.util;

/**
 *
 * @author StreamRead
 */
public class relatorios {

    /*    ConectaBanco conecta = new ConectaBanco();
    
     @SuppressWarnings("empty-statement")
     public void gerar_relatorio(String sql, String diretorio) {
     try {
     conecta.conexao();
     ResultSet rs;
     PreparedStatement stmt = conecta.conn.prepareStatement(sql);
     JRResultSetDataSource relat = new JRResultSetDataSource(rs = stmt.executeQuery());
     JasperPrint jpPrint = JasperFillManager.fillReport(diretorio, new HashMap(), relat);
     JasperViewer jv = new JasperViewer(jpPrint, false);
     jv.setVisible(true);
     jv.toFront();
     conecta.desconecta();
     } catch (JRException | SQLException e) {
     JOptionPane.showMessageDialog(null, "Erro ao tentar gerar relatório!\n " + e, "ERRO", JOptionPane.ERROR_MESSAGE);
     };
    
     }*/
}
